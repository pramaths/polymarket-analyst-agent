{
  "builds": [
    {
      "src": "backend/app/app.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "250mb" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/app/app.py"
    }
  ]
}
